//write function sum of digits that take n as input and return sum of its digits

int sumOfDigits(int n) {
    int sum = 0;

    while (n != 0) {
        sum = sum + (n % 10); // last digit add
        n = n / 10;           // remove last digit
    }

    return sum;
}
